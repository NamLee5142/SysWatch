# What the test suite needs, and nothing that ships.
#
# deploy/Install-SysWatch.ps1 builds the production virtual environment from
# requirements.txt alone, and the release archive carries that file and not
# this one. Anything added here is therefore absent from every deployment - so
# nothing importable by app/ belongs in this file.
#
# psycopg is deliberately not here. The suite runs against SQLite by default
# and only one CI job points it at PostgreSQL, which installs the driver
# itself; see "Running the suite against PostgreSQL" in README.md.
-r requirements.txt

pytest==9.1.1
respx==0.23.0

# test_workflows.py reads .github/workflows. uvicorn[standard] happens to pull
# PyYAML in as well, which is exactly why it is named here: a test that relies
# on another package's extra breaks the day that extra changes, and the
# breakage looks like a test failure rather than a missing dependency.
pyyaml==6.0.3
